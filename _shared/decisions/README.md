# ADR — quyết định liên Story

Quyết định chỉ ảnh hưởng 1 Story → `stories/<KEY>/decisions.md`.
Ảnh hưởng ≥ 2 Story hoặc kiến trúc → file ở đây, theo `0000-template.md` (MADR minimal).

Tên file: `NNNN-tieu-de.md`. Không sửa ADR `accepted`; tạo ADR mới, đổi ADR cũ sang `superseded by ADR-NNNN`.
